@model IEnumerable<ESerrano_MVC_EFProject.Models.Ref_Customer_Status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer_Status_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer_Status_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Customer_Status_Code }) |
            @Html.ActionLink("Details", "Details", new { id=item.Customer_Status_Code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Customer_Status_Code })
        </td>
    </tr>
}

</table>
